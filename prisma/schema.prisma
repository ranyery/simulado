// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-3.0.x", "linux-arm64-openssl-1.1.x", "rhel-openssl-1.0.x"]
}

datasource db {
  provider = "postgresql"
  url = "postgres://default:bg52NSMXZjGs@ep-solitary-firefly-28406931-pooler.us-east-1.postgres.vercel-storage.com:5432/verceldb?pgbouncer=true&connect_timeout=15" // uses connection pooling
  directUrl = "postgres://default:bg52NSMXZjGs@ep-solitary-firefly-28406931.us-east-1.postgres.vercel-storage.com:5432/verceldb" // uses a direct connection
  shadowDatabaseUrl = "postgres://default:bg52NSMXZjGs@ep-solitary-firefly-28406931.us-east-1.postgres.vercel-storage.com:5432/verceldb" // used for migrations
}

model Users {
  id            String    @id @default(uuid())
  email         String    @unique
  password      String

  role          String    @default("USER")
  status        String    @default("PENDING_CONFIRMATION")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @default(now())

  @@map("Users")
}

// npx prisma migrate dev --name create-users
// npx prisma studio
